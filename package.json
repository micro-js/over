{
  "name": "@f/over",
  "description": "Map a collection of functions over a value",
  "repository": "git://github.com/micro-js/over.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/map-array": "^1.2.1",
    "@f/upon": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
